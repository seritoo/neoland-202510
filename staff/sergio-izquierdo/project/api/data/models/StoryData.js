export class StoryData {
	constructor(id, ownerId, title, shortStory, storyDate) {
		this.id= id
		this.ownerId = ownerId
		this.title = title
		this.shortStory = shortStory
		this.storyDate = storyDate
	}
}
