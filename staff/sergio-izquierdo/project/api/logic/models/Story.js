export class Story {
	constructor(id, ownerId, ownerName, title, shortStory, storyDate, isOwner) {
		this.id = id
		this.ownerId = ownerId
		this.ownerName = ownerName
		this.title = title
		this.shortStory = shortStory
		this.storyDate = storyDate
		this.isOwner = isOwner
	}
}
